function refreshAdd() {
  let addWhishlistNumber = document.getElementById('wishlist-number');
  addWhishlistNumber.innerText = '<%= @counter %>';
  let wishCard = document.getElementById('wish-card-<%= @pet.id%>')
  wishCard.innerHTML = "<%= j render 'pets/wishlist', pet: @pet %>";
}

refreshAdd();
