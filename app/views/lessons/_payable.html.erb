<div class="cart">
  <div class="quantity">
      <%= simple_form_for @cart_item do |f|%>
        <%= f.input :slot, label: "Choisir un créneau", collection: @lesson.slot, input_html: {data: {"slots" => "#{@lesson.slot}"}} %>
  </div>
  <div class="button">
        <%= hidden_field_tag :lesson, @lesson.id %>
        <%= f.button :submit, "Ajouter au panier", class: "button-show" %>
      <% end %>
  </div>
  <div>
  </div>
</div>
<% if user_signed_in? %>
          <% if current_user.adminkey == "admin" %>
  <% array = [] %>
  <% i = 0 %>
  <% 100.times do |i|
    array << (Date.today + i)
    i += 1
  end %>
    <%= simple_form_for @lesson do |f|%>
      <%= f.input :slot, label: "choisir la nouvelle date", collection: array %>
      <%= hidden_field_tag "true", true%>
      <%= f.button :submit, "Ajouter", class: "button-show" %>
    <% end %>
    <%= simple_form_for @lesson do |f|%>
      <%= f.input :slot, label: "Supprimer une date", collection: @lesson.slot %>
      <%= f.button :submit, "Supprimer", class: "button-show" %>
    <% end %>
<% end %>
<% end %>

<!-- html: { id: :flatpickr }  -->
